#include <stdio.h>
/**
 * main - prints a specific sentence to the standard output
 * Return: always 0 (success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,");
return (0);
}
